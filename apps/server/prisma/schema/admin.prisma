model Admin {
    id        String   @id @map("_id") @default(cuid())
    name      String
    email     String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
    password  String

    @@unique([email])
}
