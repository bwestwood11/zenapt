model DemoRequest {
    id                String   @id @default(cuid()) @map("_id")
    firstName         String
    lastName          String
    businessName      String
    email             String
    cellPhone         String
    numberOfLocations String
    zipCode           String
    websiteUrl        String
    demoTime          DateTime
}
